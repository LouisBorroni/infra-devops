info:
  name: connection
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/users/tokens
  body:
    type: json
    data: |
      {
        "email": "test@test.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
